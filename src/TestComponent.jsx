import React from 'react';

const TestComponent = ({ headline }) => {
  return (
    <div>
      <h1>React Test Component</h1>
      <h2>{headline}</h2>
    </div>);
};

export default TestComponent;
